import Component from "$store/components/forms/ContactForm.tsx";
import type { Props } from "$store/components/forms/ContactForm.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
