import Component from "$store/components/header/HeaderFixed.tsx";
import type { Props } from "$store/components/header/HeaderFixed.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
