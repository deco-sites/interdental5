import Component from "$store/components/ui/ImageHovered.tsx";
import type { Props } from "$store/components/ui/ImageHovered.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
