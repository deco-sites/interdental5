import Component from "$store/components/ui/PrevButton.tsx";
import type { Props } from "$store/components/ui/PrevButton.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
