import Component from "$store/components/product/ProductVariantSelector.tsx";
import type { Props } from "$store/components/product/ProductVariantSelector.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
